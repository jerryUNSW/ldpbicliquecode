# Track only C++ files (.cpp, .h, .hpp, .ipp) and makefile
# Ignore everything else
*

# Allow C++ source files
!*.cpp
!*.h
!*.hpp
!*.ipp
!makefile
!.gitignore

# Allow directories to be traversed
!*/
